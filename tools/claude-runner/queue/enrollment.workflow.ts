import {workflow,node,trigger} from '@n8n/workflow-sdk';
const start=trigger({type:'n8n-nodes-base.manualTrigger',version:1,config:{name:'Enroll approved Mac key hash'}});
const database=node({type:'n8n-nodes-base.postgres',version:2.7,config:{name:'Create pilot scope only',credentials:{postgres:{id:'ULc4tEMGTKt9kicK',name:'PM OS — Postgres/Supabase'}},parameters:{operation:'executeQuery',query:"BEGIN;\n\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_jobs_v1 (\n task_id text PRIMARY KEY,\n context_hash text NOT NULL CHECK (context_hash ~ '^[a-f0-9]{64}$'),\n task jsonb NOT NULL CHECK (task->>'project' = 'dona-panel'),\n status text NOT NULL DEFAULT 'QUEUED' CHECK (status IN\n ('QUEUED','PROCESSING','RUNNING','ATTEMPT_FAILED','VERIFYING','READY_FOR_REVIEW','FAILED_QA','BLOCKED')),\n runner_id text, lease_id uuid, lease_expires_at timestamptz,\n revision bigint NOT NULL DEFAULT 0, result jsonb,\n created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_events_v1 (\n event_id text PRIMARY KEY, task_id text NOT NULL REFERENCES pm_os.dona_router_pilot_jobs_v1(task_id),\n lease_id uuid NOT NULL, revision bigint NOT NULL, record jsonb NOT NULL,\n created_at timestamptz NOT NULL DEFAULT now(), UNIQUE(task_id, revision)\n);\nALTER TABLE pm_os.dona_router_pilot_jobs_v1 ENABLE ROW LEVEL SECURITY;\nALTER TABLE pm_os.dona_router_pilot_events_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_jobs_v1, pm_os.dona_router_pilot_events_v1 FROM PUBLIC, anon, authenticated;\nSELECT true AS ok, 'pilot_schema_ready' AS status;\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_keys_v1 (\n token_hash text PRIMARY KEY CHECK(token_hash ~ '^[a-f0-9]{64}$'),\n runner_id text NOT NULL, task_id text NOT NULL, context_hash text NOT NULL,\n project text NOT NULL DEFAULT 'dona-panel' CHECK(project='dona-panel'),\n expires_at timestamptz NOT NULL, revoked boolean NOT NULL DEFAULT false,\n created_at timestamptz NOT NULL DEFAULT now()\n);\nALTER TABLE pm_os.dona_router_pilot_keys_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_keys_v1 FROM PUBLIC,anon,authenticated;\nDO $enroll$\nDECLARE result record;\nBEGIN\nEXECUTE 'WITH inserted AS (\n      INSERT INTO pm_os.dona_router_pilot_jobs_v1(task_id,context_hash,task) VALUES($1,$2,$3::jsonb)\n      ON CONFLICT(task_id) DO NOTHING RETURNING task_id\n    ) SELECT EXISTS(SELECT 1 FROM inserted) AS inserted,\n      (EXISTS(SELECT 1 FROM inserted) OR EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_jobs_v1 WHERE task_id=$1 AND context_hash=$2)) AS ok;' INTO result USING 'pilot_mac_connection_20260915','88c7563cac1510eece26830c77f2246656e844a5074d32bdc26a222331a1bfa7','{\"id\":\"pilot_mac_connection_20260915\",\"project\":\"dona-panel\",\"kind\":\"code_change\",\"goal\":\"Techniczne zadanie kontroli połączenia Mac–n8n. Nie uruchamiaj agentów ani edycji plików.\",\"baseCommit\":\"038779e66b4a21515f16ec04b13090dddfc6791a\",\"contextVersion\":1,\"files\":[\"pilot/chat.js\"],\"acceptance\":\"Status autoryzacji i odczyt dokładnie tego zadania potwierdzone. Żadnych modeli ani publikacji.\",\"executorPreference\":\"auto\"}';\nIF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'TASK_CONFLICT'; END IF;\nINSERT INTO pm_os.dona_router_pilot_keys_v1(token_hash,runner_id,task_id,context_hash,expires_at)\nVALUES('a70d530e0b27d10bb197d4e362a7b53c5993fa50318b1ce97d8020ce9b06e8f6','mac-dona-pilot','pilot_mac_connection_20260915','88c7563cac1510eece26830c77f2246656e844a5074d32bdc26a222331a1bfa7',now()+interval '24 hours')\nON CONFLICT(token_hash) DO NOTHING;\nIF NOT EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1 WHERE token_hash='a70d530e0b27d10bb197d4e362a7b53c5993fa50318b1ce97d8020ce9b06e8f6'\n  AND runner_id='mac-dona-pilot' AND task_id='pilot_mac_connection_20260915' AND context_hash='88c7563cac1510eece26830c77f2246656e844a5074d32bdc26a222331a1bfa7'\n  AND NOT revoked AND expires_at>now()) THEN RAISE EXCEPTION 'KEY_SCOPE_CONFLICT_OR_EXPIRED'; END IF;\nEND $enroll$;\nCOMMIT;\nSELECT true AS ok, runner_id,task_id,expires_at FROM pm_os.dona_router_pilot_keys_v1 WHERE token_hash='a70d530e0b27d10bb197d4e362a7b53c5993fa50318b1ce97d8020ce9b06e8f6';",options:{queryBatching:'single'}}}});
export default workflow('dona-pilot-enroll-mac','DONA — Router pilot: enroll Mac key hash').add(start).to(database);