import {workflow,node,trigger,expr} from '@n8n/workflow-sdk';
const start=trigger({type:'n8n-nodes-base.webhook',version:2.1,config:{name:'Pilot API',parameters:{httpMethod:'POST',path:'dona-router-pilot',responseMode:'responseNode',options:{}}}});
const hash=node({type:'n8n-nodes-base.code',version:2,config:{name:"Hash worker key",parameters:{mode:'runOnceForAllItems',jsCode:"const { createHash } = require('crypto');\nconst fail = code => {throw new Error(code);};\nfunction validateTask(task) {\n  if (!task || task.project !== 'dona-panel' || task.kind !== 'code_change') fail('TASK_OUT_OF_PILOT');\n  if (!['auto', 'claude', 'codex'].includes(task.executorPreference ?? 'auto')) fail('INVALID_EXECUTOR_PREFERENCE');\n  if (!/^[a-zA-Z0-9_-]{8,100}$/.test(task.id || '')) fail('INVALID_TASK_ID');\n  if (typeof task.goal !== 'string' || !task.goal.trim() || task.goal.length > 20000) fail('INVALID_GOAL');\n  if (!/^[a-f0-9]{40}$/.test(task.baseCommit || '')) fail('INVALID_BASE_COMMIT');\n  if (!Number.isSafeInteger(task.contextVersion) || task.contextVersion < 1) fail('INVALID_CONTEXT_VERSION');\n  if (!Array.isArray(task.files) || !task.files.length || task.files.length > 20) fail('INVALID_FILES');\n  const seen = new Set();\n  for (const file of task.files) {\n    // Pilot only edits UI code. Never credentials, CI, agent settings, or tests.\n    if (typeof file !== 'string' || !/^pilot\\/[a-zA-Z0-9_-]+\\.(js|css|html)$/.test(file) || seen.has(file)) fail('FILE_NOT_ALLOWED');\n    seen.add(file);\n  }\n  if (typeof task.acceptance !== 'string' || !task.acceptance.trim() || task.acceptance.length > 10000) fail('INVALID_ACCEPTANCE');\n  return task;\n}\n\nfunction contextHash(task) {\n  validateTask(task);\n  return createHash('sha256').update(JSON.stringify({\n    id: task.id, project: task.project, kind: task.kind, goal: task.goal,\n    baseCommit: task.baseCommit, contextVersion: task.contextVersion,\n    files: [...task.files].sort(), acceptance: task.acceptance,\n    executorPreference: task.executorPreference ?? 'auto',\n  })).digest('hex');\n}\n\n// Internal-only database contract. Authentication belongs to a separate gateway.\n// Never expose bootstrap/enqueue as worker operations.\n\nconst bootstrapSql = `\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_jobs_v1 (\n task_id text PRIMARY KEY,\n context_hash text NOT NULL CHECK (context_hash ~ '^[a-f0-9]{64}$'),\n task jsonb NOT NULL CHECK (task->>'project' = 'dona-panel'),\n status text NOT NULL DEFAULT 'QUEUED' CHECK (status IN\n ('QUEUED','PROCESSING','RUNNING','ATTEMPT_FAILED','VERIFYING','READY_FOR_REVIEW','FAILED_QA','BLOCKED')),\n runner_id text, lease_id uuid, lease_expires_at timestamptz,\n revision bigint NOT NULL DEFAULT 0, result jsonb,\n created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_events_v1 (\n event_id text PRIMARY KEY, task_id text NOT NULL REFERENCES pm_os.dona_router_pilot_jobs_v1(task_id),\n lease_id uuid NOT NULL, revision bigint NOT NULL, record jsonb NOT NULL,\n created_at timestamptz NOT NULL DEFAULT now(), UNIQUE(task_id, revision)\n);\nALTER TABLE pm_os.dona_router_pilot_jobs_v1 ENABLE ROW LEVEL SECURITY;\nALTER TABLE pm_os.dona_router_pilot_events_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_jobs_v1, pm_os.dona_router_pilot_events_v1 FROM PUBLIC, anon, authenticated;\nSELECT true AS ok, 'pilot_schema_ready' AS status;`;\n\nconst jobTable = 'pm_os.dona_router_pilot_jobs_v1';\nconst eventTable = 'pm_os.dona_router_pilot_events_v1';\nconst live = \"status IN ('PROCESSING','RUNNING','ATTEMPT_FAILED','VERIFYING')\";\nconst terminal = ['READY_FOR_REVIEW','FAILED_QA','BLOCKED'];\nconst requireMatch = (value, pattern) => {\n  if (typeof value !== 'string' || !pattern.test(value)) throw new Error('INVALID_QUEUE_INPUT');\n  return value;\n};\nfunction queueQuery(input) {\n  if (!input || typeof input !== 'object') throw new Error('INVALID_QUEUE_INPUT');\n  if (input.operation === 'enqueue') {\n    const task = validateTask(input.task), hash = contextHash(task);\n    // Persist only the canonical task fields; discard unrelated caller properties.\n    const canonical = Object.fromEntries(['id','project','kind','goal','baseCommit','contextVersion','files','acceptance']\n      .map(key => [key, task[key]]));\n    canonical.executorPreference = task.executorPreference ?? 'auto';\n    return { query: `WITH inserted AS (\n      INSERT INTO ${jobTable}(task_id,context_hash,task) VALUES($1,$2,$3::jsonb)\n      ON CONFLICT(task_id) DO NOTHING RETURNING task_id\n    ) SELECT EXISTS(SELECT 1 FROM inserted) AS inserted,\n      (EXISTS(SELECT 1 FROM inserted) OR EXISTS(SELECT 1 FROM ${jobTable} WHERE task_id=$1 AND context_hash=$2)) AS ok;`,\n      params: [task.id, hash, JSON.stringify(canonical)] };\n  }\n  const id = requireMatch(input.taskId, /^[a-zA-Z0-9_-]{8,100}$/);\n  const hash = requireMatch(input.contextHash, /^[a-f0-9]{64}$/);\n  if (input.operation === 'read') return {\n    query: `SELECT EXISTS(SELECT 1 FROM ${jobTable} WHERE task_id=$1 AND context_hash=$2) AS ok,\n      (SELECT row_to_json(j) FROM (SELECT task_id,context_hash,task,status,revision,result FROM ${jobTable}\n        WHERE task_id=$1 AND context_hash=$2) j) AS job;`, params: [id,hash] };\n  const runner = requireMatch(input.runnerId, /^[a-zA-Z0-9._-]{2,80}$/);\n  if (input.operation === 'acquire') return {\n    query: `WITH claimed AS (UPDATE ${jobTable} SET status='PROCESSING',runner_id=$3,\n      lease_id=gen_random_uuid(),lease_expires_at=now()+interval '120 seconds',updated_at=now()\n      WHERE task_id=$1 AND context_hash=$2 AND status='QUEUED'\n      RETURNING lease_id::text,lease_expires_at,revision)\n      SELECT EXISTS(SELECT 1 FROM claimed) AS ok, (SELECT row_to_json(claimed) FROM claimed) AS lease;`,\n    params: [id,hash,runner] };\n  const lease = requireMatch(input.leaseId, /^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$/i);\n  const owned = `task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid`;\n  const params = [id,hash,runner,lease];\n  if (input.operation === 'owns') return { query: `SELECT EXISTS(SELECT 1 FROM ${jobTable}\n    WHERE ${owned} AND ${live} AND lease_expires_at>now()) AS ok;`, params };\n  if (input.operation === 'renew') return { query: `WITH touched AS (UPDATE ${jobTable}\n    SET lease_expires_at=now()+interval '120 seconds',updated_at=now()\n    WHERE ${owned} AND ${live} AND lease_expires_at>now() RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;`, params };\n  if (input.operation === 'release') return { query: `WITH touched AS (UPDATE ${jobTable}\n    SET runner_id=NULL,lease_id=NULL,lease_expires_at=NULL,updated_at=now()\n    WHERE ${owned} AND status IN ('READY_FOR_REVIEW','FAILED_QA','BLOCKED') RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;`, params };\n  if (input.operation !== 'save') throw new Error('OPERATION_NOT_ALLOWED');\n  const record = input.record;\n  if (!record || !['RUNNING','ATTEMPT_FAILED','VERIFYING',...terminal].includes(record.status))\n    throw new Error('INVALID_QUEUE_RECORD');\n  if (record.status === 'READY_FOR_REVIEW' && (record.evidence?.passed !== true\n    || typeof record.evidence.artifactRef !== 'string' || !record.evidence.artifactRef\n    || typeof record.evidence.testsRef !== 'string' || !record.evidence.testsRef))\n    throw new Error('EVIDENCE_REQUIRED');\n  const encoded = JSON.stringify(record);\n  if (Buffer.byteLength(encoded) > 64000) throw new Error('RECORD_TOO_LARGE');\n  const eventId = requireMatch(input.eventId, /^[a-zA-Z0-9_-]{8,100}$/);\n  if (!Number.isSafeInteger(input.revision) || input.revision < 0) throw new Error('INVALID_REVISION');\n  // A single SQL statement makes status change and audit insertion atomic.\n  // Expected revision also fences concurrent writes by the same lease owner.\n  return { query: `WITH changed AS (UPDATE ${jobTable}\n    SET status=$5,result=$6::jsonb,revision=revision+1,updated_at=now()\n    WHERE ${owned} AND ${live} AND lease_expires_at>now() AND revision=$8::bigint\n      AND (($5='RUNNING' AND status IN ('PROCESSING','ATTEMPT_FAILED'))\n        OR ($5 IN ('ATTEMPT_FAILED','VERIFYING') AND status='RUNNING')\n        OR ($5 IN ('READY_FOR_REVIEW','FAILED_QA') AND status='VERIFYING')\n        OR ($5='BLOCKED'))\n    RETURNING task_id,lease_id,revision), logged AS (\n    INSERT INTO ${eventTable}(event_id,task_id,lease_id,revision,record)\n    SELECT $7,task_id,lease_id,revision,$6::jsonb FROM changed RETURNING revision)\n    SELECT EXISTS(SELECT 1 FROM logged) AS ok,(SELECT revision FROM logged) AS revision;`,\n    params: [...params,record.status,encoded,eventId,input.revision] };\n}\n\nconst keySchemaSql = `CREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_keys_v1 (\n token_hash text PRIMARY KEY CHECK(token_hash ~ '^[a-f0-9]{64}$'),\n runner_id text NOT NULL, task_id text NOT NULL, context_hash text NOT NULL,\n project text NOT NULL DEFAULT 'dona-panel' CHECK(project='dona-panel'),\n expires_at timestamptz NOT NULL, revoked boolean NOT NULL DEFAULT false,\n created_at timestamptz NOT NULL DEFAULT now()\n);\nALTER TABLE pm_os.dona_router_pilot_keys_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_keys_v1 FROM PUBLIC,anon,authenticated;`;\n\nfunction authenticateQuery(headers) {\n  const match = /^Bearer ([a-f0-9]{64})$/.exec(headers?.authorization || '');\n  if (!match) return null;\n  const digest = createHash('sha256').update(match[1]).digest('hex');\n  return { query: `SELECT EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1\n    WHERE token_hash=$1 AND NOT revoked AND expires_at>now()) AS authorized,\n    (SELECT row_to_json(k) FROM (SELECT runner_id,task_id,context_hash,project\n      FROM pm_os.dona_router_pilot_keys_v1 WHERE token_hash=$1 AND NOT revoked AND expires_at>now()) k) AS scope;`,\n    params: [digest] };\n}\n\nfunction authorizedQuery(body, auth, digest) {\n  if (auth?.authorized !== true || auth.scope?.project !== 'dona-panel') throw new Error('AUTH_FAILED');\n  if (!/^[a-f0-9]{64}$/.test(digest || '')) throw new Error('AUTH_FAILED');\n  if (!body || Array.isArray(body) || typeof body !== 'object' || Buffer.byteLength(JSON.stringify(body)) > 128 * 1024)\n    throw new Error('INVALID_REQUEST');\n  const scope = auth.scope;\n  if (!['status','read','acquire','owns','renew','save','release'].includes(body.operation))\n    throw new Error('OPERATION_NOT_ALLOWED');\n  // Missing fields are rejected rather than silently widening the task scope.\n  if (body.runnerId !== scope.runner_id || body.taskId !== scope.task_id || body.contextHash !== scope.context_hash)\n    throw new Error('SCOPE_MISMATCH');\n  if (body.operation === 'status') return { query: `SELECT EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1\n    WHERE token_hash=$1 AND runner_id=$2 AND task_id=$3 AND context_hash=$4\n      AND NOT revoked AND expires_at>now()) AS ok;`,\n    params: [digest,scope.runner_id,scope.task_id,scope.context_hash] };\n  const spec = queueQuery(body);\n  const marker = 'WHERE task_id=$1 AND context_hash=$2';\n  if (!spec.query.includes(marker)) throw new Error('UNGUARDED_QUERY');\n  const guard = ` AND EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1 k\n    WHERE k.token_hash=$${spec.params.length+1} AND k.runner_id=$${spec.params.length+2}\n    AND k.task_id=$1 AND k.context_hash=$2 AND NOT k.revoked AND k.expires_at>now())`;\n  // Fixed SQL generated by queueQuery, never SQL accepted from the request.\n  return { query: spec.query.replaceAll(marker, marker + guard), params: [...spec.params,digest,scope.runner_id] };\n}\n\nconst spec = authenticateQuery($input.first().json.headers);\nreturn [{json: spec || {query:'SELECT false AS authorized;',params:[]}}];"}}});
const authorize=node({type:'n8n-nodes-base.postgres',version:2.7,config:{name:"Check scoped key",onError:'continueRegularOutput',credentials:{postgres:{id:'ULc4tEMGTKt9kicK',name:'PM OS — Postgres/Supabase'}},parameters:{operation:'executeQuery',query:expr('{{ $json.query }}'),options:{queryBatching:'single',queryReplacement:expr('{{ $json.params }}')}}}});
const prepare=node({type:'n8n-nodes-base.code',version:2,config:{name:"Prepare scoped operation",parameters:{mode:'runOnceForAllItems',jsCode:"const { createHash } = require('crypto');\nconst fail = code => {throw new Error(code);};\nfunction validateTask(task) {\n  if (!task || task.project !== 'dona-panel' || task.kind !== 'code_change') fail('TASK_OUT_OF_PILOT');\n  if (!['auto', 'claude', 'codex'].includes(task.executorPreference ?? 'auto')) fail('INVALID_EXECUTOR_PREFERENCE');\n  if (!/^[a-zA-Z0-9_-]{8,100}$/.test(task.id || '')) fail('INVALID_TASK_ID');\n  if (typeof task.goal !== 'string' || !task.goal.trim() || task.goal.length > 20000) fail('INVALID_GOAL');\n  if (!/^[a-f0-9]{40}$/.test(task.baseCommit || '')) fail('INVALID_BASE_COMMIT');\n  if (!Number.isSafeInteger(task.contextVersion) || task.contextVersion < 1) fail('INVALID_CONTEXT_VERSION');\n  if (!Array.isArray(task.files) || !task.files.length || task.files.length > 20) fail('INVALID_FILES');\n  const seen = new Set();\n  for (const file of task.files) {\n    // Pilot only edits UI code. Never credentials, CI, agent settings, or tests.\n    if (typeof file !== 'string' || !/^pilot\\/[a-zA-Z0-9_-]+\\.(js|css|html)$/.test(file) || seen.has(file)) fail('FILE_NOT_ALLOWED');\n    seen.add(file);\n  }\n  if (typeof task.acceptance !== 'string' || !task.acceptance.trim() || task.acceptance.length > 10000) fail('INVALID_ACCEPTANCE');\n  return task;\n}\n\nfunction contextHash(task) {\n  validateTask(task);\n  return createHash('sha256').update(JSON.stringify({\n    id: task.id, project: task.project, kind: task.kind, goal: task.goal,\n    baseCommit: task.baseCommit, contextVersion: task.contextVersion,\n    files: [...task.files].sort(), acceptance: task.acceptance,\n    executorPreference: task.executorPreference ?? 'auto',\n  })).digest('hex');\n}\n\n// Internal-only database contract. Authentication belongs to a separate gateway.\n// Never expose bootstrap/enqueue as worker operations.\n\nconst bootstrapSql = `\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_jobs_v1 (\n task_id text PRIMARY KEY,\n context_hash text NOT NULL CHECK (context_hash ~ '^[a-f0-9]{64}$'),\n task jsonb NOT NULL CHECK (task->>'project' = 'dona-panel'),\n status text NOT NULL DEFAULT 'QUEUED' CHECK (status IN\n ('QUEUED','PROCESSING','RUNNING','ATTEMPT_FAILED','VERIFYING','READY_FOR_REVIEW','FAILED_QA','BLOCKED')),\n runner_id text, lease_id uuid, lease_expires_at timestamptz,\n revision bigint NOT NULL DEFAULT 0, result jsonb,\n created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_events_v1 (\n event_id text PRIMARY KEY, task_id text NOT NULL REFERENCES pm_os.dona_router_pilot_jobs_v1(task_id),\n lease_id uuid NOT NULL, revision bigint NOT NULL, record jsonb NOT NULL,\n created_at timestamptz NOT NULL DEFAULT now(), UNIQUE(task_id, revision)\n);\nALTER TABLE pm_os.dona_router_pilot_jobs_v1 ENABLE ROW LEVEL SECURITY;\nALTER TABLE pm_os.dona_router_pilot_events_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_jobs_v1, pm_os.dona_router_pilot_events_v1 FROM PUBLIC, anon, authenticated;\nSELECT true AS ok, 'pilot_schema_ready' AS status;`;\n\nconst jobTable = 'pm_os.dona_router_pilot_jobs_v1';\nconst eventTable = 'pm_os.dona_router_pilot_events_v1';\nconst live = \"status IN ('PROCESSING','RUNNING','ATTEMPT_FAILED','VERIFYING')\";\nconst terminal = ['READY_FOR_REVIEW','FAILED_QA','BLOCKED'];\nconst requireMatch = (value, pattern) => {\n  if (typeof value !== 'string' || !pattern.test(value)) throw new Error('INVALID_QUEUE_INPUT');\n  return value;\n};\nfunction queueQuery(input) {\n  if (!input || typeof input !== 'object') throw new Error('INVALID_QUEUE_INPUT');\n  if (input.operation === 'enqueue') {\n    const task = validateTask(input.task), hash = contextHash(task);\n    // Persist only the canonical task fields; discard unrelated caller properties.\n    const canonical = Object.fromEntries(['id','project','kind','goal','baseCommit','contextVersion','files','acceptance']\n      .map(key => [key, task[key]]));\n    canonical.executorPreference = task.executorPreference ?? 'auto';\n    return { query: `WITH inserted AS (\n      INSERT INTO ${jobTable}(task_id,context_hash,task) VALUES($1,$2,$3::jsonb)\n      ON CONFLICT(task_id) DO NOTHING RETURNING task_id\n    ) SELECT EXISTS(SELECT 1 FROM inserted) AS inserted,\n      (EXISTS(SELECT 1 FROM inserted) OR EXISTS(SELECT 1 FROM ${jobTable} WHERE task_id=$1 AND context_hash=$2)) AS ok;`,\n      params: [task.id, hash, JSON.stringify(canonical)] };\n  }\n  const id = requireMatch(input.taskId, /^[a-zA-Z0-9_-]{8,100}$/);\n  const hash = requireMatch(input.contextHash, /^[a-f0-9]{64}$/);\n  if (input.operation === 'read') return {\n    query: `SELECT EXISTS(SELECT 1 FROM ${jobTable} WHERE task_id=$1 AND context_hash=$2) AS ok,\n      (SELECT row_to_json(j) FROM (SELECT task_id,context_hash,task,status,revision,result FROM ${jobTable}\n        WHERE task_id=$1 AND context_hash=$2) j) AS job;`, params: [id,hash] };\n  const runner = requireMatch(input.runnerId, /^[a-zA-Z0-9._-]{2,80}$/);\n  if (input.operation === 'acquire') return {\n    query: `WITH claimed AS (UPDATE ${jobTable} SET status='PROCESSING',runner_id=$3,\n      lease_id=gen_random_uuid(),lease_expires_at=now()+interval '120 seconds',updated_at=now()\n      WHERE task_id=$1 AND context_hash=$2 AND status='QUEUED'\n      RETURNING lease_id::text,lease_expires_at,revision)\n      SELECT EXISTS(SELECT 1 FROM claimed) AS ok, (SELECT row_to_json(claimed) FROM claimed) AS lease;`,\n    params: [id,hash,runner] };\n  const lease = requireMatch(input.leaseId, /^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$/i);\n  const owned = `task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid`;\n  const params = [id,hash,runner,lease];\n  if (input.operation === 'owns') return { query: `SELECT EXISTS(SELECT 1 FROM ${jobTable}\n    WHERE ${owned} AND ${live} AND lease_expires_at>now()) AS ok;`, params };\n  if (input.operation === 'renew') return { query: `WITH touched AS (UPDATE ${jobTable}\n    SET lease_expires_at=now()+interval '120 seconds',updated_at=now()\n    WHERE ${owned} AND ${live} AND lease_expires_at>now() RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;`, params };\n  if (input.operation === 'release') return { query: `WITH touched AS (UPDATE ${jobTable}\n    SET runner_id=NULL,lease_id=NULL,lease_expires_at=NULL,updated_at=now()\n    WHERE ${owned} AND status IN ('READY_FOR_REVIEW','FAILED_QA','BLOCKED') RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;`, params };\n  if (input.operation !== 'save') throw new Error('OPERATION_NOT_ALLOWED');\n  const record = input.record;\n  if (!record || !['RUNNING','ATTEMPT_FAILED','VERIFYING',...terminal].includes(record.status))\n    throw new Error('INVALID_QUEUE_RECORD');\n  if (record.status === 'READY_FOR_REVIEW' && (record.evidence?.passed !== true\n    || typeof record.evidence.artifactRef !== 'string' || !record.evidence.artifactRef\n    || typeof record.evidence.testsRef !== 'string' || !record.evidence.testsRef))\n    throw new Error('EVIDENCE_REQUIRED');\n  const encoded = JSON.stringify(record);\n  if (Buffer.byteLength(encoded) > 64000) throw new Error('RECORD_TOO_LARGE');\n  const eventId = requireMatch(input.eventId, /^[a-zA-Z0-9_-]{8,100}$/);\n  if (!Number.isSafeInteger(input.revision) || input.revision < 0) throw new Error('INVALID_REVISION');\n  // A single SQL statement makes status change and audit insertion atomic.\n  // Expected revision also fences concurrent writes by the same lease owner.\n  return { query: `WITH changed AS (UPDATE ${jobTable}\n    SET status=$5,result=$6::jsonb,revision=revision+1,updated_at=now()\n    WHERE ${owned} AND ${live} AND lease_expires_at>now() AND revision=$8::bigint\n      AND (($5='RUNNING' AND status IN ('PROCESSING','ATTEMPT_FAILED'))\n        OR ($5 IN ('ATTEMPT_FAILED','VERIFYING') AND status='RUNNING')\n        OR ($5 IN ('READY_FOR_REVIEW','FAILED_QA') AND status='VERIFYING')\n        OR ($5='BLOCKED'))\n    RETURNING task_id,lease_id,revision), logged AS (\n    INSERT INTO ${eventTable}(event_id,task_id,lease_id,revision,record)\n    SELECT $7,task_id,lease_id,revision,$6::jsonb FROM changed RETURNING revision)\n    SELECT EXISTS(SELECT 1 FROM logged) AS ok,(SELECT revision FROM logged) AS revision;`,\n    params: [...params,record.status,encoded,eventId,input.revision] };\n}\n\nconst keySchemaSql = `CREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_keys_v1 (\n token_hash text PRIMARY KEY CHECK(token_hash ~ '^[a-f0-9]{64}$'),\n runner_id text NOT NULL, task_id text NOT NULL, context_hash text NOT NULL,\n project text NOT NULL DEFAULT 'dona-panel' CHECK(project='dona-panel'),\n expires_at timestamptz NOT NULL, revoked boolean NOT NULL DEFAULT false,\n created_at timestamptz NOT NULL DEFAULT now()\n);\nALTER TABLE pm_os.dona_router_pilot_keys_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_keys_v1 FROM PUBLIC,anon,authenticated;`;\n\nfunction authenticateQuery(headers) {\n  const match = /^Bearer ([a-f0-9]{64})$/.exec(headers?.authorization || '');\n  if (!match) return null;\n  const digest = createHash('sha256').update(match[1]).digest('hex');\n  return { query: `SELECT EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1\n    WHERE token_hash=$1 AND NOT revoked AND expires_at>now()) AS authorized,\n    (SELECT row_to_json(k) FROM (SELECT runner_id,task_id,context_hash,project\n      FROM pm_os.dona_router_pilot_keys_v1 WHERE token_hash=$1 AND NOT revoked AND expires_at>now()) k) AS scope;`,\n    params: [digest] };\n}\n\nfunction authorizedQuery(body, auth, digest) {\n  if (auth?.authorized !== true || auth.scope?.project !== 'dona-panel') throw new Error('AUTH_FAILED');\n  if (!/^[a-f0-9]{64}$/.test(digest || '')) throw new Error('AUTH_FAILED');\n  if (!body || Array.isArray(body) || typeof body !== 'object' || Buffer.byteLength(JSON.stringify(body)) > 128 * 1024)\n    throw new Error('INVALID_REQUEST');\n  const scope = auth.scope;\n  if (!['status','read','acquire','owns','renew','save','release'].includes(body.operation))\n    throw new Error('OPERATION_NOT_ALLOWED');\n  // Missing fields are rejected rather than silently widening the task scope.\n  if (body.runnerId !== scope.runner_id || body.taskId !== scope.task_id || body.contextHash !== scope.context_hash)\n    throw new Error('SCOPE_MISMATCH');\n  if (body.operation === 'status') return { query: `SELECT EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1\n    WHERE token_hash=$1 AND runner_id=$2 AND task_id=$3 AND context_hash=$4\n      AND NOT revoked AND expires_at>now()) AS ok;`,\n    params: [digest,scope.runner_id,scope.task_id,scope.context_hash] };\n  const spec = queueQuery(body);\n  const marker = 'WHERE task_id=$1 AND context_hash=$2';\n  if (!spec.query.includes(marker)) throw new Error('UNGUARDED_QUERY');\n  const guard = ` AND EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_keys_v1 k\n    WHERE k.token_hash=$${spec.params.length+1} AND k.runner_id=$${spec.params.length+2}\n    AND k.task_id=$1 AND k.context_hash=$2 AND NOT k.revoked AND k.expires_at>now())`;\n  // Fixed SQL generated by queueQuery, never SQL accepted from the request.\n  return { query: spec.query.replaceAll(marker, marker + guard), params: [...spec.params,digest,scope.runner_id] };\n}\n\nconst input = $('Pilot API').first().json;\nconst auth = $input.first().json;\nlet spec, httpStatus=200;\ntry {\n  if (auth.error) throw new Error('BACKEND_UNAVAILABLE');\n  spec = authorizedQuery(input.body,auth,$('Hash worker key').first().json.params[0]);\n} catch(error) {\n  const allowed = ['AUTH_FAILED','INVALID_REQUEST','OPERATION_NOT_ALLOWED','SCOPE_MISMATCH','BACKEND_UNAVAILABLE'];\n  const reason = allowed.includes(error.message) ? error.message : 'INVALID_REQUEST';\n  httpStatus = reason==='AUTH_FAILED' ? 401 : reason==='BACKEND_UNAVAILABLE' ? 503 : 400;\n  spec = {query:'SELECT false AS ok,$1::text AS error;',params:[reason]};\n}\nreturn [{json:{...spec,httpStatus}}];"}}});
const run=node({type:'n8n-nodes-base.postgres',version:2.7,config:{name:"Execute scoped operation",onError:'continueRegularOutput',credentials:{postgres:{id:'ULc4tEMGTKt9kicK',name:'PM OS — Postgres/Supabase'}},parameters:{operation:'executeQuery',query:expr('{{ $json.query }}'),options:{queryBatching:'single',queryReplacement:expr('{{ $json.params }}')}}}});
const format=node({type:'n8n-nodes-base.code',version:2,config:{name:"Format safe response",parameters:{mode:'runOnceForAllItems',jsCode:"const row=$input.first().json;\nconst httpStatus=$('Prepare scoped operation').first().json.httpStatus;\nif (row.error && httpStatus===200) return [{json:{httpStatus:503,body:{ok:false,error:'BACKEND_UNAVAILABLE'}}}];\nconst body={ok:row.ok===true};\nfor(const key of ['lease','job','revision','error']) if(row[key]!==undefined) body[key]=row[key];\n// pg bigint values may be strings. Accept only lossless safe nonnegative integers.\nfunction revision(value) {const n=Number(value); if(!Number.isSafeInteger(n)||n<0) throw new Error('INVALID_REVISION'); return n;}\nif(body.revision!==undefined && body.revision!==null) body.revision=revision(body.revision);\nif(body.lease) body.lease.revision=revision(body.lease.revision);\nreturn [{json:{httpStatus,body}}];"}}});
const respond=node({type:'n8n-nodes-base.respondToWebhook',version:1.5,config:{name:'Return pilot response',parameters:{respondWith:'json',responseBody:expr('{{ $json.body }}'),options:{responseCode:expr('{{ $json.httpStatus }}'),responseHeaders:{entries:[{name:'Cache-Control',value:'no-store'}]}}}}});
export default workflow('dona-router-pilot-gateway','DONA — Router pilot: scoped gateway').add(start).to(hash).to(authorize).to(prepare).to(run).to(format).to(respond);