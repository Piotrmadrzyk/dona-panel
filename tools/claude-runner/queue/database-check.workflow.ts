import { workflow, node, trigger } from '@n8n/workflow-sdk';
const start = trigger({ type: 'n8n-nodes-base.manualTrigger', version: 1, config: { name: 'Manual QA only' } });
const database = node({ type: 'n8n-nodes-base.postgres', version: 2.7, config: {
  name: 'Verify pilot queue and rollback',
  credentials: { postgres: { id: 'ULc4tEMGTKt9kicK', name: 'PM OS — Postgres/Supabase' } },
  parameters: { operation: 'executeQuery', query: "BEGIN;\n\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_jobs_v1 (\n task_id text PRIMARY KEY,\n context_hash text NOT NULL CHECK (context_hash ~ '^[a-f0-9]{64}$'),\n task jsonb NOT NULL CHECK (task->>'project' = 'dona-panel'),\n status text NOT NULL DEFAULT 'QUEUED' CHECK (status IN\n ('QUEUED','PROCESSING','RUNNING','ATTEMPT_FAILED','VERIFYING','READY_FOR_REVIEW','FAILED_QA','BLOCKED')),\n runner_id text, lease_id uuid, lease_expires_at timestamptz,\n revision bigint NOT NULL DEFAULT 0, result jsonb,\n created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE TABLE IF NOT EXISTS pm_os.dona_router_pilot_events_v1 (\n event_id text PRIMARY KEY, task_id text NOT NULL REFERENCES pm_os.dona_router_pilot_jobs_v1(task_id),\n lease_id uuid NOT NULL, revision bigint NOT NULL, record jsonb NOT NULL,\n created_at timestamptz NOT NULL DEFAULT now(), UNIQUE(task_id, revision)\n);\nALTER TABLE pm_os.dona_router_pilot_jobs_v1 ENABLE ROW LEVEL SECURITY;\nALTER TABLE pm_os.dona_router_pilot_events_v1 ENABLE ROW LEVEL SECURITY;\nREVOKE ALL ON pm_os.dona_router_pilot_jobs_v1, pm_os.dona_router_pilot_events_v1 FROM PUBLIC, anon, authenticated;\nSELECT true AS ok, 'pilot_schema_ready' AS status;\nDO $pilotcheck$\nDECLARE result record; owned_lease text; event_count integer;\nBEGIN\nEXECUTE 'WITH inserted AS (\n      INSERT INTO pm_os.dona_router_pilot_jobs_v1(task_id,context_hash,task) VALUES($1,$2,$3::jsonb)\n      ON CONFLICT(task_id) DO NOTHING RETURNING task_id\n    ) SELECT EXISTS(SELECT 1 FROM inserted) AS inserted,\n      (EXISTS(SELECT 1 FROM inserted) OR EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_jobs_v1 WHERE task_id=$1 AND context_hash=$2)) AS ok;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', '{\"id\":\"pilot_database_check_20260915\",\"project\":\"dona-panel\",\"kind\":\"code_change\",\"goal\":\"Test kolejki bez uruchamiania agenta\",\"baseCommit\":\"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\",\"contextVersion\":1,\"files\":[\"pilot/chat.js\"],\"acceptance\":\"Kontrola blokad i zapisu\",\"executorPreference\":\"auto\"}';\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'ENQUEUE_FAILED'; END IF;\nEXECUTE 'WITH inserted AS (\n      INSERT INTO pm_os.dona_router_pilot_jobs_v1(task_id,context_hash,task) VALUES($1,$2,$3::jsonb)\n      ON CONFLICT(task_id) DO NOTHING RETURNING task_id\n    ) SELECT EXISTS(SELECT 1 FROM inserted) AS inserted,\n      (EXISTS(SELECT 1 FROM inserted) OR EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_jobs_v1 WHERE task_id=$1 AND context_hash=$2)) AS ok;' INTO result USING 'pilot_database_check_20260915', '1e97f7a80d69848e2fa8f3d7e5a03dc344adb8a1032b0fc1c07cd6e036ed798b', '{\"id\":\"pilot_database_check_20260915\",\"project\":\"dona-panel\",\"kind\":\"code_change\",\"goal\":\"Conflicting payload\",\"baseCommit\":\"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\",\"contextVersion\":1,\"files\":[\"pilot/chat.js\"],\"acceptance\":\"Kontrola blokad i zapisu\",\"executorPreference\":\"auto\"}';\n    IF result.ok IS DISTINCT FROM false THEN RAISE EXCEPTION 'HASH_CONFLICT_ACCEPTED'; END IF;\nEXECUTE 'WITH claimed AS (UPDATE pm_os.dona_router_pilot_jobs_v1 SET status=''PROCESSING'',runner_id=$3,\n      lease_id=gen_random_uuid(),lease_expires_at=now()+interval ''120 seconds'',updated_at=now()\n      WHERE task_id=$1 AND context_hash=$2 AND status=''QUEUED''\n      RETURNING lease_id::text,lease_expires_at,revision)\n      SELECT EXISTS(SELECT 1 FROM claimed) AS ok, (SELECT row_to_json(claimed) FROM claimed) AS lease;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check';\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'ACQUIRE_FAILED'; END IF;\nowned_lease := result.lease->>'lease_id';\nEXECUTE 'WITH claimed AS (UPDATE pm_os.dona_router_pilot_jobs_v1 SET status=''PROCESSING'',runner_id=$3,\n      lease_id=gen_random_uuid(),lease_expires_at=now()+interval ''120 seconds'',updated_at=now()\n      WHERE task_id=$1 AND context_hash=$2 AND status=''QUEUED''\n      RETURNING lease_id::text,lease_expires_at,revision)\n      SELECT EXISTS(SELECT 1 FROM claimed) AS ok, (SELECT row_to_json(claimed) FROM claimed) AS lease;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'other-runner';\n    IF result.ok IS DISTINCT FROM false THEN RAISE EXCEPTION 'DOUBLE_CLAIM'; END IF;\nEXECUTE 'SELECT EXISTS(SELECT 1 FROM pm_os.dona_router_pilot_jobs_v1\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now()) AS ok;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease;\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'OWNERSHIP_FAILED'; END IF;\nEXECUTE 'WITH touched AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET lease_expires_at=now()+interval ''120 seconds'',updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now() RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'other-runner', owned_lease;\n    IF result.ok IS DISTINCT FROM false THEN RAISE EXCEPTION 'FOREIGN_RENEW'; END IF;\nEXECUTE 'WITH changed AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET status=$5,result=$6::jsonb,revision=revision+1,updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now() AND revision=$8::bigint\n      AND (($5=''RUNNING'' AND status IN (''PROCESSING'',''ATTEMPT_FAILED''))\n        OR ($5 IN (''ATTEMPT_FAILED'',''VERIFYING'') AND status=''RUNNING'')\n        OR ($5 IN (''READY_FOR_REVIEW'',''FAILED_QA'') AND status=''VERIFYING'')\n        OR ($5=''BLOCKED''))\n    RETURNING task_id,lease_id,revision), logged AS (\n    INSERT INTO pm_os.dona_router_pilot_events_v1(event_id,task_id,lease_id,revision,record)\n    SELECT $7,task_id,lease_id,revision,$6::jsonb FROM changed RETURNING revision)\n    SELECT EXISTS(SELECT 1 FROM logged) AS ok,(SELECT revision FROM logged) AS revision;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease, 'RUNNING', '{\"status\":\"RUNNING\"}', 'test_event_start', 0;\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'START_FAILED'; END IF;\nEXECUTE 'WITH changed AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET status=$5,result=$6::jsonb,revision=revision+1,updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now() AND revision=$8::bigint\n      AND (($5=''RUNNING'' AND status IN (''PROCESSING'',''ATTEMPT_FAILED''))\n        OR ($5 IN (''ATTEMPT_FAILED'',''VERIFYING'') AND status=''RUNNING'')\n        OR ($5 IN (''READY_FOR_REVIEW'',''FAILED_QA'') AND status=''VERIFYING'')\n        OR ($5=''BLOCKED''))\n    RETURNING task_id,lease_id,revision), logged AS (\n    INSERT INTO pm_os.dona_router_pilot_events_v1(event_id,task_id,lease_id,revision,record)\n    SELECT $7,task_id,lease_id,revision,$6::jsonb FROM changed RETURNING revision)\n    SELECT EXISTS(SELECT 1 FROM logged) AS ok,(SELECT revision FROM logged) AS revision;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease, 'VERIFYING', '{\"status\":\"VERIFYING\"}', 'test_event_stale', 0;\n    IF result.ok IS DISTINCT FROM false THEN RAISE EXCEPTION 'STALE_REVISION_ACCEPTED'; END IF;\nEXECUTE 'WITH changed AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET status=$5,result=$6::jsonb,revision=revision+1,updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now() AND revision=$8::bigint\n      AND (($5=''RUNNING'' AND status IN (''PROCESSING'',''ATTEMPT_FAILED''))\n        OR ($5 IN (''ATTEMPT_FAILED'',''VERIFYING'') AND status=''RUNNING'')\n        OR ($5 IN (''READY_FOR_REVIEW'',''FAILED_QA'') AND status=''VERIFYING'')\n        OR ($5=''BLOCKED''))\n    RETURNING task_id,lease_id,revision), logged AS (\n    INSERT INTO pm_os.dona_router_pilot_events_v1(event_id,task_id,lease_id,revision,record)\n    SELECT $7,task_id,lease_id,revision,$6::jsonb FROM changed RETURNING revision)\n    SELECT EXISTS(SELECT 1 FROM logged) AS ok,(SELECT revision FROM logged) AS revision;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease, 'VERIFYING', '{\"status\":\"VERIFYING\"}', 'test_event_verify', 1;\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'VERIFY_FAILED'; END IF;\nEXECUTE 'WITH changed AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET status=$5,result=$6::jsonb,revision=revision+1,updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now() AND revision=$8::bigint\n      AND (($5=''RUNNING'' AND status IN (''PROCESSING'',''ATTEMPT_FAILED''))\n        OR ($5 IN (''ATTEMPT_FAILED'',''VERIFYING'') AND status=''RUNNING'')\n        OR ($5 IN (''READY_FOR_REVIEW'',''FAILED_QA'') AND status=''VERIFYING'')\n        OR ($5=''BLOCKED''))\n    RETURNING task_id,lease_id,revision), logged AS (\n    INSERT INTO pm_os.dona_router_pilot_events_v1(event_id,task_id,lease_id,revision,record)\n    SELECT $7,task_id,lease_id,revision,$6::jsonb FROM changed RETURNING revision)\n    SELECT EXISTS(SELECT 1 FROM logged) AS ok,(SELECT revision FROM logged) AS revision;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease, 'READY_FOR_REVIEW', '{\"status\":\"READY_FOR_REVIEW\",\"evidence\":{\"passed\":true,\"artifactRef\":\"qa-only\",\"testsRef\":\"qa-only\"}}', 'test_event_done', 2;\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'FINISH_FAILED'; END IF;\nEXECUTE 'WITH touched AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET lease_expires_at=now()+interval ''120 seconds'',updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''PROCESSING'',''RUNNING'',''ATTEMPT_FAILED'',''VERIFYING'') AND lease_expires_at>now() RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease;\n    IF result.ok IS DISTINCT FROM false THEN RAISE EXCEPTION 'TERMINAL_RENEW_ACCEPTED'; END IF;\nEXECUTE 'WITH touched AS (UPDATE pm_os.dona_router_pilot_jobs_v1\n    SET runner_id=NULL,lease_id=NULL,lease_expires_at=NULL,updated_at=now()\n    WHERE task_id=$1 AND context_hash=$2 AND runner_id=$3 AND lease_id=$4::uuid AND status IN (''READY_FOR_REVIEW'',''FAILED_QA'',''BLOCKED'') RETURNING task_id)\n    SELECT EXISTS(SELECT 1 FROM touched) AS ok;' INTO result USING 'pilot_database_check_20260915', '1b303d0fc546fe612523527afd2995af777279f4dc0420a2bb6ab26d19e8be86', 'pilot-database-check', owned_lease;\n    IF result.ok IS DISTINCT FROM true THEN RAISE EXCEPTION 'RELEASE_FAILED'; END IF;\nSELECT count(*) INTO event_count FROM pm_os.dona_router_pilot_events_v1 WHERE task_id='pilot_database_check_20260915';\nIF event_count <> 3 THEN RAISE EXCEPTION 'AUDIT_COUNT_MISMATCH'; END IF;\nEND $pilotcheck$;\nROLLBACK;\nSELECT true AS ok, 13 AS assertions_passed, false AS production_jobs_changed, true AS rolled_back;", options: { queryBatching: 'single' } }
}, output: [{ json: { ok: true, assertions_passed: 13, production_jobs_changed: false, rolled_back: true } }] });
export default workflow('dona-router-pilot-db-check', 'DONA — Router pilot: database QA (rollback)').add(start).to(database);
